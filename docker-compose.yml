version: "3"
services:
  fb11-demo:
    image: fb11-demo:latest

  postgres:
    image: postgres:10.1
    volumes:
      - './pgdata:/var/lib/postgresql/data'
    environment:
      POSTGRES_USER: fb11
      POSTGRES_PASSWORD: fb11password

  memcached:
    image: memcached
